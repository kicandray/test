#Please Always Carry Pizza
  #pull
    #[git pull] "dowloads any changes to your repository on github
  #add [git add]
    #tells to add to repository 
  #commit [git commit]
    #saves
      #requires a message to confirm
      #-a means commit all, -m adding a message, whatever is in ""
  #Push. [git push]
    #uploads changes to github
a <- 1+1
